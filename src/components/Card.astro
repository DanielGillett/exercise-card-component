---
const { img, alt, pill, title, description, author_image, author_name, time } =
  Astro.props;
---

<article>
  <img src={img} alt={alt} />
  <p class="pill">{pill}</p>
  <h2>{title}</h2>
  <p>{description}</p>
  <div class="auther">
    <img src={author_image} alt={author_name} />
    <div>
      <p><strong>Name</strong></p>
      <p>{time}</p>
    </div>
  </div>
</article>

<style is:global>
  img {
    display: block;
    max-width: 100%;
  }
</style>

<style>
  article {
    max-width: 300px;
  }

  .author {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }

  .author img {
    width: 30px;
    border-radius: 100vmax;
  }
</style>
